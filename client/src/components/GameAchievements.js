const GameAchievements = ({achievement_count}) => {
  return (
    <div>
      <h4>{achievement_count}</h4>
      <button>Currently does nothing lol: Load Achievements eventually</button>
    </div>
  )
}

export default GameAchievements