extends layout

block content
  h1= title

  ul
    each studio in studio_list
      li
        a(href=studio.url) #{studio.studio_name}
        |  (#{studio.location})

    else
      li There are no studios.
